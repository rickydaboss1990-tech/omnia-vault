# Relay State — the baton

> Last handoff: never · by nobody · at none

> **What this is:** the single always-current statement of where this project
> stands. Read it first in every session, of either agent. Rewrite it + stamp it
> (`python scripts/relay_tool.py stamp --agent <you> --summary "..."`) before
> you finish. Full rules: [PROTOCOL.md](PROTOCOL.md).

## Now

- Fresh Cortex vault — no project initialized yet.
- Next: run the setup (`/setup` in Claude Code, or
  `python scripts/setup_vault.py --name "Your Project"`), or adopt an existing
  project (`/import` · `python scripts/setup_vault.py --inventory`).

## Just landed

- (nothing yet)

## Next

- [ ] Initialize the project (new or import).
- [ ] Ingest the first source.

## Open questions

- (none)

## Watch out

- (nothing)
